# x402-rs facilitator — build from upstream in CI (docs/11-x402-integration.md)
FROM alpine:3.20
RUN apk add --no-cache ca-certificates
EXPOSE 8402
CMD ["sh", "-c", "echo 'Run x402-rs binary with opBNB + MockUSDC config'; sleep infinity"]
