export default function ExecutorDashboardPage() {
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-semibold">Executor dashboard</h1>
      <p className="text-[var(--muted)]">Connect wallet, World ID, and browse assigned tasks (wagmi + IDKit next).</p>
    </div>
  );
}
